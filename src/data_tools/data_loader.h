#include <vector>
#include <string>

class DataLoader {
    public:
    static std::vector<std::vector<double>> loadData(const std::string &filename) {
        return std::vector<std::vector<double>>();
    }

    static std::vector<std::vector<double>> loadBinaryData(const std::string &filename) {
        return std::vector<std::vector<double>>();
    }
};