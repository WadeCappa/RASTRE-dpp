#include <vector>
#include <string>
 
class DataSaver {
    public:
    void SaveData(const std::vector<std::vector<double>> &data, const std::string &filename) {

    }

    void saveBinaryData(const std::vector<std::vector<double>> &data, const std::string &filename) {

    }
};